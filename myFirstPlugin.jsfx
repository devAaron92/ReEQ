desc:My Tone Generator

@init
// runs once when the plugin loads
frequency = 440 * pow(2, (24 - 69) / 12);
pi = 3.14159265359;
period = 2 * pi;
phaseShift = (period * frequency) / sampleRate

//sinWav = sin(angle);


@slider
// runs when the user changes a slider
//440 x 2 ^n-69/12 

@block
sampleRate = srate

@sample
// runs for every audio sample
